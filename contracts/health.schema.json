{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hogwarts.invalid/contracts/health.schema.json",
  "title": "HealthResponse",
  "type": "object",
  "additionalProperties": false,
  "required": ["status"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["alive", "started", "starting", "ready", "unavailable"]
    }
  }
}
